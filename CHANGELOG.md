# Changelog

## 0.1.0

- Initial release: per-location config entries with map or manual coordinate entry, Open-Meteo Marine + Forecast sensors, computed bathing-conditions sensor, shared global rate limiter to avoid Open-Meteo HTTP 403 across many locations.
